# N+ AI Plan

N+ AI should be a separate subsystem, not a hard-coded requirement of the compiler.

Inputs can include current `.npl` code, compiler diagnostics, symbols, AST information, documentation and project structure.

Actions: Explain, Fix, Generate, Refactor, Optimize, Test, Document.

Support both local and cloud model providers through a provider interface.
