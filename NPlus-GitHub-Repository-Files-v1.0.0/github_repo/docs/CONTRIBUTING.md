# Contributing to N+

N+ is developed by Netrix Development.

1. Create a feature branch.
2. Keep syntax changes documented in `docs/LANGUAGE_SPEC.md`.
3. Add or update an example in `examples/`.
4. Add tests for parser/compiler behavior.
5. Run the Rust formatter and tests before opening a pull request.
