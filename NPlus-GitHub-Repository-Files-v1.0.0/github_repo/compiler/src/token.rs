#[derive(Debug, Clone, PartialEq)]
pub enum Token {
    Let,
    Var,
    Const,
    Fn,
    Return,
    If,
    Else,
    While,
    For,
    In,
    True,
    False,

    Identifier(String),
    Number(f64),
    String(String),

    Plus,
    Minus,
    Star,
    Slash,
    Percent,

    Equal,
    EqualEqual,
    NotEqual,
    Greater,
    GreaterEqual,
    Less,
    LessEqual,

    And,
    Or,
    Not,

    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    Comma,
    Colon,
    Dot,
    Range,
    Bang,
    Arrow,
    Terminator,
    EOF,
}
