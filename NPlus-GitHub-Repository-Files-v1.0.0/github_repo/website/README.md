# N+ Official Website

Static, dependency-free landing page for N+ by Netrix Development.

Open `index.html` directly for a preview, or deploy the `website/` folder to GitHub Pages, Firebase Hosting, Cloudflare Pages, Netlify, or any static host.

Replace the GitHub placeholder links in `index.html` with the real N+ repository URL before publishing.
